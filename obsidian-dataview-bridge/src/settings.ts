export interface MetadataAPISettings {
    // Currently no settings needed for the database approach
    // Keep interface for future extensibility
}

export const DEFAULT_SETTINGS: MetadataAPISettings = {
    // Empty for now
};